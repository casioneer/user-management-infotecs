# User Management System

Приложение для управления пользователями с авторизацией и CRUD операциями.

## 🚀 Технологический стек

- **React 16** - библиотека для построения UI
- **TypeScript** - типизированный JavaScript
- **Webpack** - сборщик модулей
- **Ant Design 5** - UI библиотека компонентов
- **React Router 6** - маршрутизация
- **TanStack Query 4** - управление серверным состоянием
- **Axios** - HTTP клиент
- **Day.js** - работа с датами
- **FSD (Feature-Sliced Design)** - архитектура проекта

## 📁 Структура проекта (FSD)

```
src/
├── app/                    # Инициализация приложения
│   └── App.tsx            # Провайдеры (Router, QueryClient, ConfigProvider)
├── pages/                 # Страницы приложения
│   ├── login/            # Страница авторизации
│   ├── users/            # Страница списка пользователей
│   └── not-found/        # Страница 404
├── features/             # Бизнес-функции
│   ├── auth/            # Авторизация
│   │   ├── api/         # API для авторизации
│   │   └── ui/          # LoginForm, LogoutButton
│   └── user-management/ # Управление пользователями
│       └── ui/          # UserModal (создание/редактирование)
├── entities/            # Бизнес-сущности
│   └── user/           # Сущность пользователя
│       ├── api/        # API для работы с пользователями
│       └── model/      # Типы и интерфейсы
├── shared/             # Переиспользуемый код
│   ├── api/           # Базовая настройка axios
│   └── lib/           # Утилиты (token, constants)
└── widgets/           # Композитные блоки (не используется в данном проекте)
```

## 🔧 Установка и запуск

### Предварительные требования

- Node.js >= 14
- npm >= 6

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm start
```

Приложение откроется автоматически по адресу [http://localhost:3000](http://localhost:3000)

### Сборка для production

```bash
npm run build
```

Результат сборки будет в папке `dist/`

## 🔐 Авторизация

Для входа в систему используйте:

- **Логин:** `admin`
- **Пароль:** `admin`

После успешной авторизации токен сохраняется в `localStorage`.

## 📋 Функциональность

### Страница авторизации (`/login`)
- Форма входа с валидацией
- Имитация запроса к серверу (2000ms задержка)
- Уведомления об ошибках
- Блокировка кнопки во время загрузки
- Автоматический редирект авторизованного пользователя

### Страница пользователей (`/users`)
- Список пользователей в виде карточек
- Отображение аватара, имени и даты регистрации
- Кнопка "Выход" для разлогинивания
- Кнопка "Создать пользователя"
- Клик по карточке/аватару открывает редактирование

### Модальное окно создания/редактирования
- Поля: ID (disabled при редактировании), Имя, Аватар
- Валидация пустых полей
- Валидация корректности URL
- Блокировка UI во время API запросов
- Кнопки: Создать/Сохранить, Удалить, Отмена

### Страница 404
- Отображается при переходе на несуществующий роут
- Использует компонент `Result` из Ant Design

## 🌐 API

Проект использует [MockAPI.io](https://mockapi.io) для имитации backend:

**Base URL:** `https://698369929c3efeb892a5ab39.mockapi.io/api/v1`

### Endpoints:
- `GET /users` - получить список пользователей
- `POST /users` - создать пользователя
- `PUT /users/:id` - обновить пользователя
- `DELETE /users/:id` - удалить пользователя

## 🎨 Особенности реализации

- **React 16** - используется `ReactDOM.render` вместо `createRoot`
- **TanStack Query** - кеширование, автоматическая перезагрузка данных
- **Оптимистичные обновления** - `queryClient.invalidateQueries` после мутаций
- **Защищенные роуты** - проверка токена перед доступом к `/users`
- **Форматирование дат** - `dayjs` с форматом `DD.MM.YYYY`

## 📝 Git workflow

Проект разрабатывался с использованием feature-веток:

1. `feature/login` - реализация авторизации
2. `feature/users` - реализация управления пользователями

Каждая ветка мержилась в `main` через Pull Request.

## 👤 Автор

@casioneer
